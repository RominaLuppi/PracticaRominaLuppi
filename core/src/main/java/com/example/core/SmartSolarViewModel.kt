package com.example.core

/*
import androidx.lifecycle.ViewModel
import androidx.lifecycle.viewModelScope
import com.example.domain.SmartDetalleTab
import com.example.domain.SmartRepository
import dagger.hilt.android.lifecycle.HiltViewModel
import kotlinx.coroutines.flow.MutableStateFlow
import kotlinx.coroutines.flow.asStateFlow
import kotlinx.coroutines.launch
import javax.inject.Inject
*/

/*
@HiltViewModel
class SmartSolarViewModel @Inject constructor(
    private val repository : SmartRepository) : ViewModel() {

    private val _detalle = MutableStateFlow<SmartDetalleTab?>(null)
    val detalle = _detalle.asStateFlow()

    init {
        viewModelScope.launch {
            _detalle.value = repository.getSmartDetalle()
        }
    }

}*/
